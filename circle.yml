machine:
  node:
    version: 8.9.4
dependencies:
  post:
    - bundle install
    - bundle exec jekyll build

deployment:
  prod:
    branch: master
    commands:
      - firebase deploy --token=$FIREBASE_TOKEN --non-interactive
